import { IInsurance } from '../models/Insurance';

// Dummy function for now
const calculateInsurancePrice = (insurance: IInsurance): Number => {
  return 200;
};

export { calculateInsurancePrice };
